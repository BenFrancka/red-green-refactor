//function returns new array with all items from original array and a new item pushed to the end
const copyAndPush = (someArray, newItem) => {
    return [...someArray, newItem]
}

export default copyAndPush;